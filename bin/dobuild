#!/bin/bash

#
# DOBUILD 
# 	Build an image from current folder and tag as folder name
#

if [ -f Dockerfile ] ; then
	docker build -t $(basename $PWD) .
else
	echo "Missing Dockerfile in $PWD"
	exit 5
fi
